var expect = require('chai').expect;
var SearchRepository = require('../app/repository.js');
var sinon = require('sinon');

describe("SearchRepository", function () {
	it("should pass", function() {
		expect(true).to.be.true;
	})
});